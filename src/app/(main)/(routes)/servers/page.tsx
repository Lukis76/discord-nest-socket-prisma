import { FC } from 'react';

interface ServersPageProps {
  // Define prop types here
}

const ServersPage: FC<ServersPageProps> = () => {
  return (
    <div>
      <h1>Servers</h1>
    </div>
  );
};

export default ServersPage;